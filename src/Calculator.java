import java.math.BigDecimal;

/**Calculator Interface that is used in the simpleCalculator class.
 *
 */
public interface Calculator {
    double add();
    double subtract();
    double multiply();
    double divide();
    double root();
    double power();
}
